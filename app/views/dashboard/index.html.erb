<!DOCTYPE html>
<html lang="<%= I18n.locale -%>">
  <head>
    <title> <%= h(yield(:title) || "Foreman") %></title>
    <%= stylesheet_link_tag 'normalize' %>
    <%= stylesheet_link_tag 'bootstrap' %>
    <%= stylesheet_link_tag 'style' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
    <%= javascript_tag "var AUTH_TOKEN = #{form_authenticity_token.inspect};" if protect_against_forgery? %>
    <%= javascript_tag "var URL_PREFIX = '#{request.script_name}';" %>
    <%= javascript_tag "var I18N_MARK = true;" if SETTINGS[:mark_translated] and not Rails.env.test? %>
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="viewport" content="user-scalable=no, width=device-width, initial-scale=1.0, maximum-scale=1.0" />
    <%= favicon_link_tag "favicon.ico"%>
    <%= yield(:head) %>
  </head>

  <body>

    <script type="text/x-handlebars">
      <div class="navbar">
        <div class="navbar-inner">
          <a class="brand" href="#">ForemanJS</a>
          <ul class="nav">
            <li>{{#linkTo 'domains'}}Domains{{/linkTo}}</li>
            <li>{{#linkTo 'about'}}About{{/linkTo}}</li>
          </ul>
        </div>
      </div>
      {{ outlet }}
    </script>

    <script type="text/x-handlebars" id="about">
      <div class='about'>
        <p>ForemanJS is a testbed for the JSON API.</p>
        <p>The purpose is ...</p>
      </div>
    </script>

    <script type="text/x-handlebars" id="domains" data-template-name='domains'>
      <div class="container-fluid">
        <div class="row-fluid">
          <div class="span3">
            <table class="table">
              <thread>
                <tr><th>Domains</th></tr>
              </thread>
              {{#each model}}
                <tr><td>
                  {{#linkTo 'domain' this}}{{name}}{{/linkTo}}
                </td></tr>
              {{/each}}
            </table>
          </div>
          <div class="span8">
            {{outlet}}
          </div>
        </div>
      </div>
    </script>

    <script type="text/x-handlebars" id="domains/index">
      <h3 class="text-warning">Please select a domain</h3>
    </script>

    <script type="text/x-handlebars" id="domain">
      <h1>{{name}}</h1>
      <h2>by {{createdAt}}<h2>
    </script>

  </body>
</html>
